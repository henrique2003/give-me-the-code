'use client'

import styled from 'styled-components'

export const Logo = styled.p`
  font-size: 30px;
  font-weight: 500;
  color: #76DBF3;
  font-weight: bold;

  span {
    color: white;
  }

  @media(max-width: 800px) {
    font-size: 25px;
  }
`
